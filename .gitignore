/target

# Local test material — real game dumps, never committed
/test_titles/

# Local dev scratch (staged bases, extracted references, sample output)
/.dev/

# User-supplied secrets and keys — never committed
*.bin
keys.txt
*.key

# Build output
/out/
*.nfs
*.app
*.h3

# Editor / OS
.DS_Store

# Claude Code project state and scratch PR-body files (see CLAUDE.md's PR workflow)
/.claude/
/pr-*.md

# Local sample injects (build artifacts)
/build_*/
